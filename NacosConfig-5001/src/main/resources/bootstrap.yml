server:
  port: 5001

spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      config:
        server-addr: localhost:8848 #配置中心服务端地址
        file-extension: yaml  #指定配置文件扩展名
  profiles:
    active: dev  #指定配置文件所属环境

